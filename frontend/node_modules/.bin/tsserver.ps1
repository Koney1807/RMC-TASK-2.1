#!/usr/bin/env pwsh
$basedir=Split-Path $MyInvocation.MyCommand.Definition -Parent

$exe=""
if ($PSVersionTable.PSVersion -lt "6.0" -or $IsWindows) {
  $exe=".exe"
}
$ret=0
if (Test-Path "$basedir/node$exe") {
  if ($MyInvocation.ExpectingInput) {
    $input | & "$basedir/node$exe"  "$basedir/../.deno/typescript@5.9.3/node_modules/typescript/bin/tsserver" $args
  } else {
    & "$basedir/node$exe"  "$basedir/../.deno/typescript@5.9.3/node_modules/typescript/bin/tsserver" $args
  }
  $ret=$LASTEXITCODE
} else {
  if ($MyInvocation.ExpectingInput) {
    $input | & "node$exe"  "$basedir/../.deno/typescript@5.9.3/node_modules/typescript/bin/tsserver" $args
  } else {
    & "node$exe"  "$basedir/../.deno/typescript@5.9.3/node_modules/typescript/bin/tsserver" $args
  }
  $ret=$LASTEXITCODE
}
exit $ret
