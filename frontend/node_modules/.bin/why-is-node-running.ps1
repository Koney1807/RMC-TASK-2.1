#!/usr/bin/env pwsh
$basedir=Split-Path $MyInvocation.MyCommand.Definition -Parent

$exe=""
if ($PSVersionTable.PSVersion -lt "6.0" -or $IsWindows) {
  $exe=".exe"
}
$ret=0
if (Test-Path "$basedir/node$exe") {
  if ($MyInvocation.ExpectingInput) {
    $input | & "$basedir/node$exe"  "$basedir/../.deno/why-is-node-running@2.3.0/node_modules/why-is-node-running/cli.js" $args
  } else {
    & "$basedir/node$exe"  "$basedir/../.deno/why-is-node-running@2.3.0/node_modules/why-is-node-running/cli.js" $args
  }
  $ret=$LASTEXITCODE
} else {
  if ($MyInvocation.ExpectingInput) {
    $input | & "node$exe"  "$basedir/../.deno/why-is-node-running@2.3.0/node_modules/why-is-node-running/cli.js" $args
  } else {
    & "node$exe"  "$basedir/../.deno/why-is-node-running@2.3.0/node_modules/why-is-node-running/cli.js" $args
  }
  $ret=$LASTEXITCODE
}
exit $ret
